import { render } from '@testing-library/react-native';

import HomeScreen from '@/app/screens/GameScreen';

// describe('<HomeScreen />', () => {
//     test('タイトルが表示されていること', () => {
//         const {getByText} = render(<HomeScreen stageNum={1} changeEnable={(isEnable) => {}} />);

//         getByText("ステージ1");
//     });
// });